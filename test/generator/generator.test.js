const expect  = require('chai').expect;
const gen = require("../../src/generator/generator");

/*
describe('Test generator.js', function() {
    describe('Test simple boolean data', function() {
        var boolTmpl = {
            dataType : "boolean",
            desc : {
                policy: "true"
            }
        };
        var config = {};
        it('Generate one result', function() {
            expect(gen.generate(boolTmpl, 1, config)).to.be.ok;
        });
        it('Generate 5 results', function() {
            expect(gen.generate(boolTmpl, 5, config)).to.deep.equal([true, true, true, true, true]);
        });
    });
});
*/
